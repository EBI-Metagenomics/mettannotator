config {
    testsDir "."
    workDir System.getenv("NFT_WORKDIR") ?: ".nf-test"
    configFile "nextflow.config"

    // nf-core module tests are upstream code — exclude them from local runs
    ignore 'modules/nf-core/**/*', 'subworkflows/nf-core/**/*'
}
